apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

# 覆盖镜像 tag，例如：
# images:
#   - name: t8y2/dbx
#     newTag: "1.2.3"
